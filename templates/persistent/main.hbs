<div class="group primary">
    <div class="app panel">
        {{#if hasActor}}
        {{> (partial 'level')}}
        {{/if}}
    </div>
    <div class="app panel saves">
        {{> (partial 'statistics') list=saves}}
        {{> (partial 'statistics') list=others}}
    </div>
</div>

<div class="group infos">
    <div class="app panel">
        {{> (partial 'infos') infoSections=infoSections}}
    </div>
</div>